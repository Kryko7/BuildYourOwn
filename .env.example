# Copy to .env and fill in. `.env` is git-ignored: it holds personal settings only.

# Whose journey this is. Shown as "<name>'s Journey" across the site and `byo status`.
# Leave empty (or delete the line) and everything reads "The Journey" instead.
PUBLIC_JOURNEY_OWNER=
