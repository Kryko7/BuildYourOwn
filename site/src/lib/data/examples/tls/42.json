[
	{
		"title": "A line before the keys rotate",
		"kind": "wire",
		"request": "`rotate\\n` under application_traffic_secret_0",
		"request_hex": "17030300181e062b20793da4201d531595651a734d73684babe64923fe",
		"response": "`etator\\n` back. A `18 00 00 01 00` KeyUpdate follows, and then the same exchange works under secret_1.",
		"response_hex": "1703030018dcb2f156a779f0944472f5882e507961bdb77401ac65c1ee",
		"note": "The KeyUpdate message is five bytes: type 24, a uint24 length of 1, and the request_update byte. Everything else about it is derivation.",
		"request_fields": [
			{
				"offset": 0,
				"length": 1,
				"field": "record.type",
				"value": "application_data(23)"
			},
			{
				"offset": 1,
				"length": 2,
				"field": "record.legacy_record_version",
				"value": "0x0303"
			},
			{
				"offset": 3,
				"length": 2,
				"field": "record.length",
				"value": "24 (bytes that follow)"
			},
			{
				"offset": 5,
				"length": 24,
				"field": "record.encrypted_record",
				"value": "24 bytes: the AEAD ciphertext plus its 16-byte tag. The real content type is inside it, after the content and before the padding.",
				"varies": true
			}
		],
		"response_fields": [
			{
				"offset": 0,
				"length": 1,
				"field": "record.type",
				"value": "application_data(23)"
			},
			{
				"offset": 1,
				"length": 2,
				"field": "record.legacy_record_version",
				"value": "0x0303"
			},
			{
				"offset": 3,
				"length": 2,
				"field": "record.length",
				"value": "24 (bytes that follow)"
			},
			{
				"offset": 5,
				"length": 24,
				"field": "record.encrypted_record",
				"value": "24 bytes: the AEAD ciphertext plus its 16-byte tag. The real content type is inside it, after the content and before the padding.",
				"varies": true
			}
		]
	},
	{
		"title": "What a KeyUpdate changes, and what it does not",
		"kind": "text",
		"request": "secret_(N+1) = HKDF-Expand-Label(secret_N, \"traffic upd\", \"\", Hash.length)\nkey, iv      = HKDF-Expand-Label(secret_(N+1), \"key\"/\"iv\", \"\", ...)\nsequence number -> 0",
		"request_hex": "",
		"response": "Only the sender's direction moves. The transcript is untouched, the master secret is untouched, and no new key exchange happens.",
		"response_hex": "",
		"note": "update_requested(1) asks the peer to rotate too, and its answer must say update_not_requested(0). Two peers that both say 1 would never stop.",
		"request_fields": [],
		"response_fields": []
	}
]
