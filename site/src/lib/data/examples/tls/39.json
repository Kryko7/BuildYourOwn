[
	{
		"title": "What each of the fifty connections does",
		"kind": "wire",
		"request": "A full handshake, then `conn00\\n` as application data, then close_notify",
		"request_hex": "170303001809505c31cd7a80514b571ef5d6dffc248917ceef05f8bc98",
		"response": "`00nnoc\\n`, and a close_notify back.",
		"response_hex": "170303001897115c96ad28817d38ed4e5546a6f4b6dd0c62d05efdf2b9",
		"note": "Fifty of these at once. Nothing about any one of them is unusual — the test is whether the fiftieth is as correct as the first.",
		"request_fields": [
			{
				"offset": 0,
				"length": 1,
				"field": "record.type",
				"value": "application_data(23)"
			},
			{
				"offset": 1,
				"length": 2,
				"field": "record.legacy_record_version",
				"value": "0x0303"
			},
			{
				"offset": 3,
				"length": 2,
				"field": "record.length",
				"value": "24 (bytes that follow)"
			},
			{
				"offset": 5,
				"length": 24,
				"field": "record.encrypted_record",
				"value": "24 bytes: the AEAD ciphertext plus its 16-byte tag. The real content type is inside it, after the content and before the padding.",
				"varies": true
			}
		],
		"response_fields": [
			{
				"offset": 0,
				"length": 1,
				"field": "record.type",
				"value": "application_data(23)"
			},
			{
				"offset": 1,
				"length": 2,
				"field": "record.legacy_record_version",
				"value": "0x0303"
			},
			{
				"offset": 3,
				"length": 2,
				"field": "record.length",
				"value": "24 (bytes that follow)"
			},
			{
				"offset": 5,
				"length": 24,
				"field": "record.encrypted_record",
				"value": "24 bytes: the AEAD ciphertext plus its 16-byte tag. The real content type is inside it, after the content and before the padding.",
				"varies": true
			}
		]
	},
	{
		"title": "Serial and parallel are both fine",
		"kind": "text",
		"request": "Fifty sockets opened before any of them has finished its handshake",
		"request_hex": "",
		"response": "A threaded server serves them at once; a single-threaded one serves them from the listen backlog in turn. Both finish all fifty.",
		"response_hex": "",
		"note": "The reference is single-threaded, so this suite never requires two handshakes to be in flight at the same moment — only that opening fifty sockets does not lose any of them.",
		"request_fields": [],
		"response_fields": []
	}
]
